<#div class="favorites text-center bg-light p-3 pb-0 m-0">
  <#% @favorites.each do |f| %>
    <#% @university = favorite.university %>
    <#div class="d-inline-flex justify-content-between">
      <#%= render "shared/card-universityfav", object: @university %>
    <#/div>
  <#% end %>
<#/div>
